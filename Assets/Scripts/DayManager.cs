using UnityEngine;

// Stores global information related to day and semester progression
public class DayManager : MonoBehaviour
{
// Tracks the current day within the current semester
public static int currentDay = 1;


// Tracks the current semester
public static int currentSemester = 1;

// Defines how many days are in a semester
public static int daysPerSemester = 30;


}
